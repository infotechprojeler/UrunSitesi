@model IEnumerable<Slider>

@{
    ViewData["Title"] = "Slider Resimleri";
}

<p>
    <a asp-action="Create" class="btn btn-outline-primary">
        Yeni Ekle
        <i class="fa-solid fa-plus"></i>
    </a>
</p>
<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    <img src="/Images/@item.Image" height="41" alt="Resim Yok" title="@item.Image" />
                </td>
                <td class="text-nowrap">
                    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.Id" title="Düzenle">
                        <i class="fa-solid fa-pencil"></i>
                    </a> |
                    <a class="btn btn-sm btn-warning" asp-action="Details" asp-route-id="@item.Id" title="Detay">
                        <i class="fa-regular fa-window-restore"></i>
                    </a> |
                    <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@item.Id" title="Sil">
                        <i class="fa-solid fa-trash-can"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
